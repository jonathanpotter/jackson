1. Start a bash console.
2. Read the `man` pages on the `os-release`.
3. Try to find the `os-release` file by using `ls` at the locations mentioned in the man pages.
4. View the contents of the `os-release` file using `nano`, `cat`, `more`, and `less` commands.
5. Go back to the man pages for `os-release` and note the example python code that is given to read the `os-release` file. Copy it.
6. Create a new python program named `WhatOs.py` with `nano` and paste the example python code. Exit `nano` and save the file.
7. Run your new python program with `python WhatOs.py`. Did it work?
